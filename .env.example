# ─── Claude Dev VM — environment variables ───────────────────────────────────
# Copy this file to .env and uncomment the lines you want to use.
# Changes take effect on the next VM start (./stop.sh && ./up.sh).

# Host directory to mount inside the VM at /home/claude/shared (virtio-9p).
# SHARED_FOLDER=/path/to/your/project

# Comma-separated TCP ports to forward from host to VM (in addition to SSH).
# Services are reachable at localhost:<port> on the host.
# FORWARDED_PORTS=3000,3001,5173

# VM resources. Defaults: 8192 MB RAM, 4 vCPUs.
# VM_MEMORY=8192
# VM_CPUS=4
